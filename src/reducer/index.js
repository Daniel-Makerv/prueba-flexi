import {
  
} from '../actions/constants.js';

const initialState = {
   
}
export default function reducer(state = initialState, { type, payload }) {
    switch (type) {
        case "":
            return state;
        default:
            return state;
    }
}

